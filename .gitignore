.claude/
.DS_Store
node_modules/

# fluid parser: the review sheets are PLAINTEXT VW data — never commit them to the
# public repo (only the obfuscated docs/fluids/*.json is meant to ship). Source PDFs
# stay off the repo too.
tools/fluids-review/
*.pdf
